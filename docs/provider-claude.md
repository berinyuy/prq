# Claude Provider

The default provider is the local Claude Code CLI.

## Command

```bash
claude -p "<prompt>" --output-format json --json-schema schemas/review_plan.schema.json
```

Configure via `~/.prq/config.yaml` if the command or args differ.
